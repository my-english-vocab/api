name: Home Server Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - home-server

    steps:
      - name: Show runner info
        run: |
          hostname
          whoami
          uname -m
          docker --version
