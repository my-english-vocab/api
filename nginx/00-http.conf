server {
    listen 80;
    server_name api.myenglishvocab.com;

    access_log /dev/stdout safe_main;
    error_log /dev/stderr warn;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}