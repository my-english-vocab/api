spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  web:
    error:
      include-message: never

jwt:
  secret: ${JWT_SECRET}

auth:
  cookie:
    secure: true

# 운영 프론트 Origin만 허용 (예시)
# cors:
#   allowed-origins:
#     - https://your-app.vercel.app
